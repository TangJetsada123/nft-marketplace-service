export enum DATE {
  DAY = 'day',
  WEEK = 'week',
  MONTH = 'month',
  YEAR = 'year',
}

export enum Role {
  USER = 'user',
  ADMIN = 'admin',
}

export enum STATUS {
  OWNED = 'Owned',
  ONSALE = 'On Sale',
}
